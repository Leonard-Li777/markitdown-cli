name: Build

on:
  workflow_dispatch:
    inputs:
      build_win32:
        description: 'Build Windows x64'
        type: boolean
        default: true
      build_linux_x64:
        description: 'Build Linux x64'
        type: boolean
        default: true
      build_linux_arm64:
        description: 'Build Linux arm64'
        type: boolean
        default: true
      build_darwin_x64:
        description: 'Build macOS x64 (disabled)'
        type: boolean
        default: false
      build_darwin_arm64:
        description: 'Build macOS arm64'
        type: boolean
        default: true
      create_release:
        description: 'Publish to GitHub Release'
        type: boolean
        default: false

jobs:
  select:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        run: |
          entries='[]'
          append() { entries=$(echo "$entries" | jq -c --argjson item "$1" '. + [$item]'); }
          [ "${{ inputs.build_win32 }}" = "true" ] && append '{"platform":"win32" ,"arch":"x64"  ,"ext":"zip"   ,"runner":"windows-latest"}'
          [ "${{ inputs.build_linux_x64 }}" = "true" ] && append '{"platform":"linux"  ,"arch":"x64"  ,"ext":"tar.gz","runner":"ubuntu-latest"}'
          [ "${{ inputs.build_linux_arm64 }}" = "true" ] && append '{"platform":"linux"  ,"arch":"arm64","ext":"tar.gz","runner":"ubuntu-24.04-arm"}'
          # [ "${{ inputs.build_darwin_x64 }}" = "true" ] && append '{"platform":"darwin" ,"arch":"x64"  ,"ext":"tar.gz","runner":"macos-13"}'
          [ "${{ inputs.build_darwin_arm64 }}" = "true" ] && append '{"platform":"darwin" ,"arch":"arm64","ext":"tar.gz","runner":"macos-latest"}'
          echo "matrix=$entries" >> "$GITHUB_OUTPUT"

  build:
    needs: select
    if: needs.select.outputs.matrix != '[]'
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.select.outputs.matrix) }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 120

    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive
          fetch-depth: 1

      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          pip install pyinstaller pytesseract
          pip install -e markitdown/packages/markitdown
          pip install -e markitdown/packages/markitdown-ocr

      - name: Install patchelf (Linux)
        if: startsWith(matrix.platform, 'linux')
        run: sudo apt-get update && sudo apt-get install -y patchelf

      - name: Show PyInstaller version
        run: python3 -m PyInstaller --version

      - name: Build
        env:
          # Avoid onnxruntime device scan noise/hangs during PyInstaller analysis
          ORT_DISABLE_DEVICE_DISCOVERY: "1"
        run: python scripts/build.py --skip-deps --skip-tesseract --skip-exiftool

      - name: Debug base_library.zip
        shell: bash
        run: |
          ZIP=""
          for candidate in dist/_internal/base_library.zip dist/markitdown/_internal/base_library.zip; do
            if [ -f "$candidate" ]; then ZIP="$candidate"; break; fi
          done
          if [ -n "$ZIP" ]; then
            python3 -c "import zipfile; z=zipfile.ZipFile('$ZIP'); n=sorted(z.namelist()); h=any(x.startswith('encodings/') for x in n); print('zip:', '$ZIP'); print('encodings in zip:', h); [print(' ', x) for x in n[:40]]"
          else
            echo "no base_library.zip (checked dist/_internal/ and dist/markitdown/_internal/)"
            find dist -name 'base_library.zip' 2>/dev/null || true
          fi

      - name: Smoke test
        shell: bash
        run: |
          echo "=== Check binary exists ==="
          if [ "${{ matrix.platform }}" = "win32" ]; then
            EXE="dist/markitdown.exe"
          else
            EXE="dist/markitdown"
          fi
          test -f "$EXE" || { echo "FAIL: $EXE not found"; exit 1; }

          echo "=== Check libpython is bundled ==="
          if [ "${{ matrix.platform }}" = "linux" ]; then
            if ! find dist/ -name 'libpython3*.so*' | grep -q .; then
              echo "FAIL: libpython .so not found in dist/"
              exit 1
            fi
          elif [ "${{ matrix.platform }}" = "darwin" ]; then
            # macOS: accept libpython*.dylib (standard) or Python3/Python (framework build)
            if ! find dist/ -name 'libpython3*.dylib' -o -name 'Python3' -o -name 'Python' | grep -q .; then
              echo "FAIL: libpython (.dylib / Python3 / Python) not found in dist/"
              exit 1
            fi
          fi

          echo "=== Check certifi CA bundle is bundled ==="
          if ! find dist/ -name 'cacert.pem' | grep -q .; then
            echo "WARN: cacert.pem not found — HTTPS may fail"
          fi

          echo "=== Check encodings data dir ==="
          find dist/ -type d -name 'encodings' 2>/dev/null || echo "no encodings dir found"

          echo "=== Check encodings in base_library.zip ==="
          ZIP=""
          for candidate in dist/_internal/base_library.zip dist/markitdown/_internal/base_library.zip; do
            if [ -f "$candidate" ]; then ZIP="$candidate"; break; fi
          done
          if [ -n "$ZIP" ]; then
            python3 -c "import zipfile,sys; z=zipfile.ZipFile('$ZIP'); ok=any(n.startswith('encodings/') for n in z.namelist()); print('encodings in zip:', ok); sys.exit(0 if ok else 1)"
          else
            echo "FAIL: base_library.zip not found"
            exit 1
          fi

          echo "=== Run --help ==="
          "$EXE" --help

      - name: Create tar.gz archive
        if: matrix.ext == 'tar.gz'
        run: tar -czf "markitdown-bin-${{ matrix.platform }}-${{ matrix.arch }}.tar.gz" -C dist .

      - name: Create zip archive
        if: matrix.ext == 'zip'
        shell: pwsh
        run: |
          $name = "markitdown-bin-${{ matrix.platform }}-${{ matrix.arch }}"
          Compress-Archive -Path "dist/*" -DestinationPath "$name.zip" -CompressionLevel Optimal

      - uses: actions/upload-artifact@v5
        with:
          name: markitdown-bin-${{ matrix.platform }}-${{ matrix.arch }}
          path: markitdown-bin-${{ matrix.platform }}-${{ matrix.arch }}.${{ matrix.ext }}

  release:
    if: always() && fromJSON(inputs.create_release)
    needs: [select, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Generate tag
        id: tag
        run: echo "name=v$(date +%Y%m%d%H%M%S)" >> "$GITHUB_OUTPUT"

      - uses: actions/download-artifact@v5
        continue-on-error: true
        with:
          path: release-assets

      - name: List assets
        run: find release-assets -type f

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.name }}
          files: release-assets/**/*
          generate_release_notes: true
